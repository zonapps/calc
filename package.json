
{
  "name": "calc",
  "private": true,
  "version": "0.0.0",
  "description": "A programmer's calculator",
  "repository": "https://github.com/zonapps/calc",
  "devDependencies": {
    "bower": "^1.3.1",
    "http-server": "^0.6.1"
  },
  "scripts": {
    "prestart": "npm install",
    "start": "http-server -a localhost -p 8000"
  }
}
